{
  "name": "@blueshift-ui/tokens",
  "version": "0.12.0",
  "description": "BlueshiftUI design tokens",
  "homepage": "https://github.com/varsitytutors/blueshift-ui/tree/master/packages/tokens#readme",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "devDependencies": {
    "style-dictionary": "3.7.2"
  },
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "transpile": "rm -rf {tmp,dist} && node ./lib/style-dictionary/build.mjs && tsc && copyfiles -u 1 tmp/**/*.css dist/ && rm -rf tmp"
  }
}
