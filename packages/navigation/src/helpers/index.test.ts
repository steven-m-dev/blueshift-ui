import { scrollContentIntoView } from './';

describe('package index', () => {
  describe('scrollContentIntoView', () => {
    it('is defined', () => {
      expect(scrollContentIntoView).toBeDefined();
    });
  });
});
