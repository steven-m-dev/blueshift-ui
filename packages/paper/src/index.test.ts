import Default, { Paper } from './';

describe('package index', () => {
  describe('<Default />', () => {
    it('is defined', () => {
      expect(Default).toBeDefined();
    });
  });

  describe('<Paper />', () => {
    it('is defined', () => {
      expect(Paper).toBeDefined();
    });
  });
});
