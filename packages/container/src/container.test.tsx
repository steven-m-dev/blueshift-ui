import Container from './container';
import React from 'react';
import { render } from '@testing-library/react';

describe('<Container />', () => {
  it('renders', () => {
    render(<Container />);
  });
});
