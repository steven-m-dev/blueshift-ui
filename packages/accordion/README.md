# @blueshift-ui/accordion

The accordion component allows the user to show and hide sections of related content on a page. 

Example usage:

```jsx
import Accordion from "@blueshift-ui/accordion";

function Component() {
  return (
    <Accordion title="Title">Content</Accordion>
  );
}

```
