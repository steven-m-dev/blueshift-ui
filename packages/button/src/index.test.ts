import { AsyncActionButton, Button, IconButton } from './';

describe('package index', () => {
  describe('<Button />', () => {
    it('is defined', () => {
      expect(Button).toBeDefined();
    });
  });

  describe('<AsyncActionButton />', () => {
    it('is defined', () => {
      expect(AsyncActionButton).toBeDefined();
    });
  });

  describe('<IconButton />', () => {
    it('is defined', () => {
      expect(IconButton).toBeDefined();
    });
  });
});
